<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/LinearLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:baselineAligned="false"
    android:orientation="vertical"
    tools:context=".Home_Activity" >


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="horizontal" >

            <Button
                android:id="@+id/btn_play_activity"
                android:layout_width="0dp"
                android:layout_height="148dp"
                android:layout_weight="0.64"
                android:background="@android:color/transparent"
                android:drawablePadding="20dp"
                android:drawableTop="@drawable/ic_game_activity"
                android:onClick="game_activity"
                android:paddingTop="32sp"
                android:text="@string/ictext_play"
                android:textColor="#000000" />
        </LinearLayout>

        <fragment
            android:id="@+id/firstGameGroupFragment"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginTop="?android:attr/actionBarSize"
            android:layout_weight="1"
            class="com.example.android.rssfeed.MyListFragment" >
        </fragment>

        <fragment
            android:id="@+id/secondGameGroupFragment"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            class="com.example.android.rssfeed.DetailFragment" >

            <!-- Preview: layout=@layout/details -->
        </fragment>

        <fragment
            android:id="@+id/thirdGameGroupFragment"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            class="com.example.android.rssfeed.DetailFragment" >

            <!-- Preview: layout=@layout/details -->
        </fragment>

</LinearLayout>